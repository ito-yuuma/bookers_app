<div class="index">
  <h1 class="book">Editing Book</h1>

  <%= form_with model: @book do |f| %>

    <p class="te">Title</p>
    <%= f.text_field :title %>

    <p class="te bd">Body</p>
    <%= f.text_area :body %>

    <%= f.submit 'Update Book', class:"submit" %>

  <% end %>
  <div class="urls">
  <%= link_to "Show", book_path(@book.id),class: "try editer" %>
  <div class="text">|</div>
  <%= link_to "Back", books_path,class: "a1 booker" %>
  </div>
</div>